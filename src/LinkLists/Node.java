package LinkLists;

public class Node<E> {
    //elem variable is of the generic type
    //a node has a reference to its next and previous nodes for a doubly linked list
    E elem;
    Node<E> next;
    Node<E> prev;
}
